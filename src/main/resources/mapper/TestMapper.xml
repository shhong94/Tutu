<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.study.home.TestMapper">
 
    <select id="test" resultType="string">
        SELECT
        		NAME
        FROM
        		MEMBER1
        WHERE 
        		ID = 'hong'
    </select>
 
</mapper>